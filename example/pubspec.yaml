name: varioqub_plugin_example
description: "Demonstrates how to use the varioqub_plugin plugin."
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  appmetrica_plugin: ^3.2.0
  flutter:
    sdk: flutter
  varioqub_plugin:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
